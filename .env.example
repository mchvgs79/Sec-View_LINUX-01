# Where the controller (server) lives
CONTROLLER_URL=http://localhost:8080

# Heartbeat interval in ms (default 30000 = 30s)
HEARTBEAT=30000

# Optional: where to keep small state files (fingerprint, token)
# AGENT_STATE_DIR=C:\sec-viewer\agent-state

VIEWER_BASE=http://localhost:8080